-- Basic CopilotChat setup - DISABLED (using enhanced agent version in copilot-agent.lua)
return {
  -- This is disabled to avoid conflicts with the enhanced agent version
  -- All CopilotChat functionality is now in copilot-agent.lua
}
